/v1/bank/list:
  get:
    tags:
      - Bank
    summary: Get all bank information
    description: Get all bank information
    operationId: getAllBank

    parameters: []
    responses:
      "200":
        description: successful operation
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/v1GetBanksResponse"
            examples:
              successExample:
                $ref: "#/components/examples/v1GetBanksResponse"
